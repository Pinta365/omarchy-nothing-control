# Working notes: measured facts, dead ends and protocol archaeology. Useful
# locally, but they are a lab notebook rather than part of the plugin, so the
# durable findings live in README.md instead.
knowledge/

# Locally confirmed device mappings, written by tools/map-device.sh.
helper/models.local.json

# Python bytecode from running the helper or its tests in place.
__pycache__/
*.py[cod]

# Editor and OS droppings.
*.swp
*~
.DS_Store

# Files coding agents load on their own. Everything in the repo ships to users'
# plugin folders, so these stay local; docs/contributing.md is the shared copy.
AGENTS.md
CLAUDE.md
